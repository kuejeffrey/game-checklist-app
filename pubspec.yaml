name: level_up
description: A cozy daily survival & productivity tracker with game-like rewards.
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  device_timezone: ^1.0.0
  flutter_local_notifications: ^18.0.1
  shared_preferences: ^2.2.2
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
